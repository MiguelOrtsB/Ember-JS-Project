import Component from '@glimmer/component';
import { inject as service } from '@ember/service';

export default class BookingListComponent extends Component {
  //     @service Bookings;
  //     bookings = this.Bookings.bookingList;
  //
}
